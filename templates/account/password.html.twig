{% extends 'base.html.twig' %}

{% block title %}Modifier mon mot de passe - ArcheType{% endblock %}

{% block body %}
	<div class="container pt-5 mt-5">
		<h1 id="titre">Modifier mon mot de passe</h1>
		<a href="{{ path('account') }}">Retourner sur mon compte
		</a>
		{% if notif %}
			<div class="alert alert-info">{{ notif }}</div>
		{% endif %}
        {{ form(form) }}
	</div>

{% endblock %}

